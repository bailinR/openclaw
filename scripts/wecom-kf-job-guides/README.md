# 企业微信招聘岗位提问规则

每个岗位一个 Markdown 文件。bridge 启动时读取本目录下的 `*.md`，根据候选人本次消息、最近对话和已沉淀资料里的岗位文本匹配“岗位别名”。

文件格式：

```md
# 岗位名称

岗位别名：销售、销售岗、商务、客户经理

这里写这个岗位的提问顺序、评分口径和注意事项。
```

改已有岗位要求：直接编辑对应 `.md` 后重启 bridge。

新增岗位：复制一个 `.md`，改标题、岗位别名和内容后重启 bridge。
